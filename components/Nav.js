import React from "react";
import styles from "../styles/Nav.module.scss";

const Nav = () => {
  return (
    <nav className={styles.navbar}>
      <a href="/">Blog</a>
    </nav>
  );
};

export default Nav;
